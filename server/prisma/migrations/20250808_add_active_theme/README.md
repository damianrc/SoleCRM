Migration: Add activeTheme field to User model for theme preference.

- Adds a new field `activeTheme` to the User model in the Prisma schema.
- Allows storing the user's preferred theme (e.g., 'light' or 'dark').
